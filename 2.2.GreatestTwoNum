#include <iostream>

using namespace std;

int main()
{
    
    int a,b;
    cin>>a>>b;
    cout<<"Greatest between "<<a<<" and "<<b<<" is "<<max(a,b);
    
    /*
    if(a>b){
        cout<<"Greatest between "<<a<<" and "<<b<<" is "<<a;
    }
    else{
        cout<<"Greatest between "<<a<<" and "<<b<<" is "<<b;
    }
    */
    
    return 0;
}
